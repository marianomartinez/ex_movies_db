<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies filtered</title>
</head>

<body>
    <h1>Movies with <%= sendActor.first_name %> <%= sendActor.last_name %></h1>
    <ul>
        <% filteredMovies.forEach(movie => { %>
        <li><a href="/movies/detail/<%= movie.id %>"><%= movie.id %> - <%= movie.title %></a></li>
        <% }); %>
    </ul>
    <p><a href="/movies"><button>Back to movie index</button></a></p>
</body>

</html>